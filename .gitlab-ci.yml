
build-job:
  stage: build
  script:
    - echo "Hello, $GITLAB_USER_LOGIN!"
  environment:
    name: Dev
  only:
    - master

test-job1:
  stage: test
  script:
    - echo "This job tests something"
  only:
    - master
    
deploy-prod:
  stage: deploy
  script:
    - echo "deply stage"
    - cp -rf ./ /root/front
    - cd /root/front
    - sed -i 's/localhost/137.184.8.39/' ./src/net/http.ts
    - npm install
    - sudo systemctl restart  ecefront.service
  only:
    - master
